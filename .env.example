# LLM provider — set at least one key to use /api/fanout/generate
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional overrides
# LLM_PROVIDER=gemini            # or: openai (default: inferred from keys)
# LLM_MODEL=gemini-2.5-flash     # or: gpt-4o-mini
# EMBEDDING_MODEL=all-MiniLM-L6-v2
# SIMILARITY_THRESHOLD=0.72
